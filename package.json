{
  "name": "cortex",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"cd server && npx tsx watch --ignore '../node_modules/**' src/index.ts\" \"cd client && npx vite\"",
    "dev:server": "cd server && npx tsx watch --ignore '../node_modules/**' src/index.ts",
    "dev:client": "cd client && npx vite",
    "build": "cd client && npx tsc -b && npx vite build",
    "start": "cd server && npx tsx src/index.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}